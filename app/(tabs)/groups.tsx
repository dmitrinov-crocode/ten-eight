import { Text } from 'react-native';
import { ScreenContainer } from '@/components';

export default function GroupsScreen() {
  return (
    <ScreenContainer title="Groups" scrollable>
      <Text style={{ color: 'white' }}>Groups</Text>
    </ScreenContainer>
  );
}
